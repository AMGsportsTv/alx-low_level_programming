#include <stdio.h>

/**
  *main - The printf command does not print a new line.
  *
  *Return: The printf prints the out without a new line.
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
