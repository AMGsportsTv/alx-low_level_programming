#include <stdio.h>

/**
 * main - The puts command prints the results to a standard out with a new line
 *
 * Return: Always 0 (success)
 */
int main(void)
{
    puts("\"Programming is like building a multilingual puzzle");
    return (0);
}
